name: Terraform CI

on:
  push:
    branches:
      - main
    paths:
      - 'terraform/**'
      - '.github/workflows/terraform.yml'

jobs:
  terraform:
    runs-on: ubuntu-latest

    env:
      TF_VAR_public_subnet_1a_cidr: ${{ secrets.TF_VAR_public_subnet_1a_cidr }}
      TF_VAR_public_subnet_1b_cidr: ${{ secrets.TF_VAR_public_subnet_1b_cidr }}
      TF_VAR_private_subnet_1a_cidr: ${{ secrets.TF_VAR_private_subnet_1a_cidr }}
      TF_VAR_private_subnet_1b_cidr: ${{ secrets.TF_VAR_private_subnet_1b_cidr }}
      TF_VAR_app_port: ${{ secrets.TF_VAR_app_port }}
      TF_VAR_hosted_zone_name: ${{ secrets.TF_VAR_hosted_zone_name }}
      TF_VAR_app_domain_name: ${{ secrets.TF_VAR_app_domain_name }}
      TF_VAR_uploads_bucket_name: ${{ secrets.TF_VAR_uploads_bucket_name }}
      TF_VAR_alert_email: ${{ secrets.TF_VAR_alert_email }}
      TF_VAR_lambda_function_name: ${{ secrets.TF_VAR_lambda_function_name }}

    defaults:
      run:
        working-directory: terraform/environments/prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure Terraform Cloud credentials
        run: |
          mkdir -p ~/.terraform.d
          cat > ~/.terraform.d/credentials.tfrc.json <<EOF
          {
            "credentials": {
              "app.terraform.io": {
                "token": "${{ secrets.TF_API_TOKEN }}"
              }
            }
          }
          EOF

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Terraform Init
        run: terraform init -input=false

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -input=false